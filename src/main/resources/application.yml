spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mcm_showcase?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 4886

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true